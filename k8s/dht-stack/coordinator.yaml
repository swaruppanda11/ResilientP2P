apiVersion: apps/v1
kind: Deployment
metadata:
  name: coordinator
  namespace: p2p-dht
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coordinator
  template:
    metadata:
      labels:
        app: coordinator
    spec:
      nodeSelector:
        building: a
      containers:
      - name: coordinator
        image: REGISTRY/coordinator:v1
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: p2p-common-config
        securityContext:
          capabilities:
            add: ["NET_ADMIN"]
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: coordinator
  namespace: p2p-dht
spec:
  selector:
    app: coordinator
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
