apiVersion: apps/v1
kind: Deployment
metadata:
  name: dht-bootstrap
  namespace: p2p-coordinator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dht-bootstrap
  template:
    metadata:
      labels:
        app: dht-bootstrap
    spec:
      nodeSelector:
        building: a
      containers:
      - name: dht-bootstrap
        image: us-central1-docker.pkg.dev/resilientp2p-492916/resilientp2p/dht-bootstrap:a250c05
        ports:
        - containerPort: 6000
          protocol: UDP
        env:
        - name: DHT_PORT
          value: "6000"
        - name: DHT_KSIZE
          value: "5"
        - name: DHT_ALPHA
          value: "3"
        securityContext:
          capabilities:
            add: ["NET_ADMIN"]
---
apiVersion: v1
kind: Service
metadata:
  name: dht-bootstrap
  namespace: p2p-coordinator
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: dht-bootstrap
  ports:
  - port: 6000
    targetPort: 6000
    protocol: UDP
